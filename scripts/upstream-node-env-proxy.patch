--- upstream/network-proxy/src/proxy.rs
+++ upstream/network-proxy/src/proxy.rs
@@ -372,6 +372,7 @@
     PROXY_ACTIVE_ENV_KEY,
     ALLOW_LOCAL_BINDING_ENV_KEY,
     ELECTRON_GET_USE_PROXY_ENV_KEY,
+    "NODE_USE_ENV_PROXY",
     "HTTP_PROXY",
     "HTTPS_PROXY",
     "http_proxy",
@@ -526,6 +527,9 @@
         "true".to_string(),
     );
 
+    // Node.js 22.21+ / 24+: make built-in fetch() respect HTTP_PROXY/HTTPS_PROXY
+    env.insert("NODE_USE_ENV_PROXY".to_string(), "1".to_string());
+
     // Keep HTTP_PROXY/HTTPS_PROXY as HTTP endpoints. A lot of clients break if
     // those vars contain SOCKS URLs. We only switch ALL_PROXY here.
     //
