--- upstream/linux-sandbox/src/bwrap.rs	2026-05-12 21:49:51
+++ upstream/linux-sandbox/src/bwrap.rs	2026-05-12 21:51:56
@@ -109,6 +109,7 @@
     pub preserved_files: Vec<File>,
     pub synthetic_mount_targets: Vec<SyntheticMountTarget>,
     pub protected_create_targets: Vec<ProtectedCreateTarget>,
+    pub used_tmpfs_root: bool,
 }
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
@@ -249,6 +250,7 @@
                 preserved_files: Vec::new(),
                 synthetic_mount_targets: Vec::new(),
                 protected_create_targets: Vec::new(),
+                used_tmpfs_root: false,
             })
         } else {
             Ok(create_bwrap_flags_full_filesystem(command, options))
@@ -290,6 +292,7 @@
         preserved_files: Vec::new(),
         synthetic_mount_targets: Vec::new(),
         protected_create_targets: Vec::new(),
+        used_tmpfs_root: false,
     }
 }
 
@@ -306,6 +309,7 @@
         preserved_files,
         synthetic_mount_targets,
         protected_create_targets,
+        used_tmpfs_root,
     } = create_filesystem_args(
         file_system_sandbox_policy,
         sandbox_policy_cwd,
@@ -330,6 +334,11 @@
         args.push("--proc".to_string());
         args.push("/proc".to_string());
     }
+    // Deny-default writes: make tmpfs root read-only after all mounts.
+    if used_tmpfs_root {
+        args.push("--remount-ro".to_string());
+        args.push("/".to_string());
+    }
     if normalized_command_cwd.as_path() != command_cwd {
         // Bubblewrap otherwise inherits the helper's logical cwd, which can be
         // a symlink alias that disappears once the sandbox only mounts
@@ -345,6 +354,7 @@
         preserved_files,
         synthetic_mount_targets,
         protected_create_targets,
+        used_tmpfs_root,
     })
 }
 
@@ -437,6 +447,7 @@
     unreadable_roots.sort();
     unreadable_roots.dedup();
 
+    let mut used_tmpfs_root = false;
     let args = if file_system_sandbox_policy.has_full_disk_read_access() {
         // Read-only root, then mount a minimal device tree.
         // In bubblewrap (`bubblewrap.c`, `SETUP_MOUNT_DEV`), `--dev /dev`
@@ -453,6 +464,7 @@
     } else {
         // Start from an empty filesystem and add only the approved readable
         // roots plus a minimal `/dev`.
+        used_tmpfs_root = true;
         let mut args = vec![
             "--tmpfs".to_string(),
             "/".to_string(),
@@ -515,6 +527,7 @@
         preserved_files: Vec::new(),
         synthetic_mount_targets: Vec::new(),
         protected_create_targets: Vec::new(),
+        used_tmpfs_root,
     };
     let mut allowed_write_paths = Vec::with_capacity(writable_roots.len());
     for writable_root in &writable_roots {
--- upstream/linux-sandbox/src/linux_run_main.rs	2026-05-12 21:49:51
+++ upstream/linux-sandbox/src/linux_run_main.rs	2026-05-12 21:52:19
@@ -393,6 +393,7 @@
         preserved_files: bwrap_args.preserved_files,
         synthetic_mount_targets: bwrap_args.synthetic_mount_targets,
         protected_create_targets: bwrap_args.protected_create_targets,
+        used_tmpfs_root: bwrap_args.used_tmpfs_root,
     })
 }
 
@@ -501,6 +502,7 @@
         preserved_files,
         synthetic_mount_targets,
         protected_create_targets,
+        used_tmpfs_root: _,
     } = bwrap_args;
     let setup_signal_mask = ForwardedSignalMask::block();
     let synthetic_mount_registrations = register_synthetic_mount_targets(&synthetic_mount_targets);
@@ -1303,6 +1305,7 @@
         preserved_files,
         synthetic_mount_targets,
         protected_create_targets,
+        used_tmpfs_root: _,
     } = bwrap_args;
     let setup_signal_mask = ForwardedSignalMask::block();
     let synthetic_mount_registrations = register_synthetic_mount_targets(&synthetic_mount_targets);
