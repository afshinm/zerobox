diff --color -ruN a/upstream/network-proxy/src/certs.rs b/upstream/network-proxy/src/certs.rs
--- a/upstream/network-proxy/src/certs.rs	2026-03-30 10:06:45
+++ b/upstream/network-proxy/src/certs.rs	2026-03-30 10:06:45
@@ -109,14 +109,8 @@
 fn load_or_create_ca() -> Result<(String, String)> {
     let (cert_path, key_path) = managed_ca_paths()?;
 
-    if cert_path.exists() || key_path.exists() {
-        if !cert_path.exists() || !key_path.exists() {
-            return Err(anyhow!(
-                "both managed MITM CA files must exist (cert={}, key={})",
-                cert_path.display(),
-                key_path.display()
-            ));
-        }
+    // Fast path: both files already exist. No lock needed.
+    if cert_path.exists() && key_path.exists() {
         validate_existing_ca_key_file(&key_path)?;
         let cert_pem = fs::read_to_string(&cert_path)
             .with_context(|| format!("failed to read CA cert {}", cert_path.display()))?;
@@ -125,33 +119,47 @@
         return Ok((cert_pem, key_pem));
     }
 
-    if let Some(parent) = cert_path.parent() {
-        fs::create_dir_all(parent)
-            .with_context(|| format!("failed to create {}", parent.display()))?;
+    // Slow path: acquire an exclusive advisory lock to serialize CA creation
+    // across concurrent processes. This is the same pattern Cargo uses for
+    // concurrent builds (flock on Unix, LockFileEx on Windows).
+    let parent = cert_path
+        .parent()
+        .ok_or_else(|| anyhow!("missing parent directory for CA cert"))?;
+    fs::create_dir_all(parent)
+        .with_context(|| format!("failed to create {}", parent.display()))?;
+
+    let lock_path = parent.join("ca.lock");
+    let lock_file = File::create(&lock_path)
+        .with_context(|| format!("failed to open CA lock file {}", lock_path.display()))?;
+    lock_file
+        .lock()
+        .with_context(|| format!("failed to acquire lock on {}", lock_path.display()))?;
+    // Lock released when lock_file is dropped (end of function).
+
+    // Re-check after acquiring the lock — another process may have created
+    // the files while we were waiting.
+    if cert_path.exists() && key_path.exists() {
+        validate_existing_ca_key_file(&key_path)?;
+        let cert_pem = fs::read_to_string(&cert_path)
+            .with_context(|| format!("failed to read CA cert {}", cert_path.display()))?;
+        let key_pem = fs::read_to_string(&key_path)
+            .with_context(|| format!("failed to read CA key {}", key_path.display()))?;
+        return Ok((cert_pem, key_pem));
     }
-    if let Some(parent) = key_path.parent() {
-        fs::create_dir_all(parent)
-            .with_context(|| format!("failed to create {}", parent.display()))?;
-    }
 
+    // We won the lock. Generate and write.
     let (cert_pem, key_pem) = generate_ca()?;
-    // The CA key is a high-value secret. Create it atomically with restrictive permissions.
-    // The cert can be world-readable, but we still write it atomically to avoid partial writes.
-    //
-    // We intentionally use create-new semantics: if a key already exists, we should not overwrite
-    // it silently (that would invalidate previously-trusted cert chains).
     write_atomic_create_new(&key_path, key_pem.as_bytes(), /*mode*/ 0o600)
         .with_context(|| format!("failed to persist CA key {}", key_path.display()))?;
     if let Err(err) = write_atomic_create_new(&cert_path, cert_pem.as_bytes(), /*mode*/ 0o644)
         .with_context(|| format!("failed to persist CA cert {}", cert_path.display()))
     {
-        // Avoid leaving a partially-created CA around (cert missing) if the second write fails.
         let _ = fs::remove_file(&key_path);
         return Err(err);
     }
-    let cert_path = cert_path.display();
-    let key_path = key_path.display();
-    info!("generated MITM CA (cert_path={cert_path}, key_path={key_path})");
+    let cert_display = cert_path.display();
+    let key_display = key_path.display();
+    info!("generated MITM CA (cert_path={cert_display}, key_path={key_display})");
     Ok((cert_pem, key_pem))
 }
 
diff --color -ruN a/upstream/network-proxy/src/http_proxy.rs b/upstream/network-proxy/src/http_proxy.rs
--- a/upstream/network-proxy/src/http_proxy.rs	2026-03-30 10:06:45
+++ b/upstream/network-proxy/src/http_proxy.rs	2026-03-30 10:06:45
@@ -330,11 +330,13 @@
         return Ok(());
     };
 
-    if mode == NetworkMode::Limited
-        && upgraded
-            .extensions()
-            .get::<Arc<mitm::MitmState>>()
-            .is_some()
+    // MITM intercepts HTTPS traffic to inspect/modify headers. Required for:
+    // - Limited mode: enforcing method restrictions on inner requests
+    // - Any mode with header transformer: secret placeholder substitution
+    if upgraded
+        .extensions()
+        .get::<Arc<mitm::MitmState>>()
+        .is_some()
     {
         let host = normalize_host(&target.host.to_string());
         let port = target.port;
@@ -737,6 +739,12 @@
 
     // Strip hop-by-hop headers only after extracting metadata used for policy correlation.
     remove_hop_by_hop_request_headers(req.headers_mut());
+
+    // Secret substitution: replace placeholder tokens with real values for matching hosts.
+    if let Some(transformer) = app_state.header_transformer() {
+        transformer.transform_headers(req.headers_mut(), &host);
+    }
+
     match client.serve(req).await {
         Ok(resp) => Ok(resp),
         Err(err) => {
diff --color -ruN a/upstream/network-proxy/src/lib.rs b/upstream/network-proxy/src/lib.rs
--- a/upstream/network-proxy/src/lib.rs	2026-03-30 10:06:45
+++ b/upstream/network-proxy/src/lib.rs	2026-03-30 10:06:45
@@ -42,6 +42,7 @@
 pub use runtime::ConfigReloader;
 pub use runtime::ConfigState;
 pub use runtime::NetworkProxyState;
+pub use runtime::RequestHeaderTransformer;
 pub use state::NetworkProxyAuditMetadata;
 pub use state::NetworkProxyConstraintError;
 pub use state::NetworkProxyConstraints;
diff --color -ruN a/upstream/network-proxy/src/mitm.rs b/upstream/network-proxy/src/mitm.rs
--- a/upstream/network-proxy/src/mitm.rs	2026-03-30 10:06:45
+++ b/upstream/network-proxy/src/mitm.rs	2026-03-30 10:06:45
@@ -214,6 +214,11 @@
         .headers
         .insert(HOST, HeaderValue::from_str(&authority)?);
 
+    // Secret substitution: replace placeholder tokens with real values for matching hosts.
+    if let Some(transformer) = request_ctx.policy.app_state.header_transformer() {
+        transformer.transform_headers(&mut parts.headers, &target_host);
+    }
+
     let inspect = mitm.inspect_enabled();
     let max_body_bytes = mitm.max_body_bytes();
     let body = if inspect {
diff --color -ruN a/upstream/network-proxy/src/runtime.rs b/upstream/network-proxy/src/runtime.rs
--- a/upstream/network-proxy/src/runtime.rs	2026-03-30 10:06:45
+++ b/upstream/network-proxy/src/runtime.rs	2026-03-30 10:06:45
@@ -196,11 +196,19 @@
     }
 }
 
+/// Optional callback for transforming request headers before forwarding
+/// (e.g., secret placeholder substitution). Implemented outside the proxy
+/// crate to keep application-specific logic separate.
+pub trait RequestHeaderTransformer: Send + Sync + 'static {
+    fn transform_headers(&self, headers: &mut rama_http::HeaderMap, target_host: &str);
+}
+
 pub struct NetworkProxyState {
     state: Arc<RwLock<ConfigState>>,
     reloader: Arc<dyn ConfigReloader>,
     blocked_request_observer: Arc<RwLock<Option<Arc<dyn BlockedRequestObserver>>>>,
     audit_metadata: NetworkProxyAuditMetadata,
+    header_transformer: Option<Arc<dyn RequestHeaderTransformer>>,
 }
 
 impl std::fmt::Debug for NetworkProxyState {
@@ -218,6 +226,7 @@
             reloader: self.reloader.clone(),
             blocked_request_observer: self.blocked_request_observer.clone(),
             audit_metadata: self.audit_metadata.clone(),
+            header_transformer: self.header_transformer.clone(),
         }
     }
 }
@@ -268,6 +277,7 @@
             reloader,
             blocked_request_observer: Arc::new(RwLock::new(blocked_request_observer)),
             audit_metadata,
+            header_transformer: None,
         }
     }
 
@@ -281,6 +291,14 @@
 
     pub fn audit_metadata(&self) -> &NetworkProxyAuditMetadata {
         &self.audit_metadata
+    }
+
+    pub fn set_header_transformer(&mut self, t: Arc<dyn RequestHeaderTransformer>) {
+        self.header_transformer = Some(t);
+    }
+
+    pub fn header_transformer(&self) -> Option<&Arc<dyn RequestHeaderTransformer>> {
+        self.header_transformer.as_ref()
     }
 
     pub async fn current_cfg(&self) -> Result<NetworkProxyConfig> {
