{
  "$schema": "./schema.json",
  "description": "OpenCode binary directory (Landlock requires directory read to execute binaries).",
  "platform": "linux",
  "allow_read": [
    "$HOME/.opencode/bin"
  ]
}
